# 前端环境配置示例文件
# 复制此文件为 .env 或 .env.local 并修改相应的配置值

# API 请求前缀（默认：/api）
# 生产环境通常使用相对路径，会自动匹配当前域名和协议
VITE_API_PREFIX=/api

# 如果需要指定完整的 API 地址（不推荐，会失去自动协议匹配）
# VITE_API_PREFIX=https://www.wdmlzffonline.top/api

# 应用标题
VITE_APP_TITLE=服装库存管理系统

# 注意：
# 1. 文件访问 URL 由后端返回，前端无需配置 FILE_BASE_URL
# 2. 后端会在返回数据中包含完整的 url 字段（如：https://www.wdmlzffonline.top/uploads/xxx.jpg）
# 3. 前端直接使用后端返回的 url 字段即可
